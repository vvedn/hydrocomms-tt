# Tiny Tapeout project information
project:
  title:        "FSK Modem"      # Project title
  author:       "Vaishnavi V"      # Your name
  discord:      "vaishnavi05051"      # Your discord username, for communication and automatically assigning you a Tapeout role (optional)
  description:  "FSK modem for underwater acoustic communication (46-56 kHz, 100 bps)" 
  language:     "Verilog" # other examples include SystemVerilog, Amaranth, VHDL, etc
  clock_hz:     50000000       # Clock frequency in Hz (or 0 if not applicable)

  # How many tiles your design occupies? A single tile is about 340x160 uM.
  tiles: "1x2"          # Valid values: 1x1, 1x2, 2x2, 3x2, 4x2, 3x4 or 4x4

  # Your top module name must start with "tt_um_". Make it unique by including your github username:
  top_module:  "tt_um_hydrocomms"

  # List your project's source files here.
  # Source files must be in ./src and you must list each source file separately, one per line.
  # Don't forget to also update `PROJECT_SOURCES` in test/Makefile.
  source_files:
    - "project.v"
    - "spi.v"
    - "spi_cmd.v"
    - "sample_gen.v"
    - "tx_handler.v"
    - "rx.v"
    - "nco.v"
    - "frame_builder.v"
    - "goertzel.v"
    - "bit_slicer.v"
    - "frame_sync.v"
    - "regs.v"
    - "crc.v"

# The pinout of your project. Leave unused pins blank. DO NOT delete or add any pins.
# This section is for the datasheet/website. Use descriptive names (e.g., RX, TX, MOSI, SCL, SEG_A, etc.).
pinout:
  # Inputs
  ui[0]: "SPI_SCLK"
  ui[1]: "SPI_MOSI"
  ui[2]: "SPI_CS_N"
  ui[3]: ""
  ui[4]: ""
  ui[5]: ""
  ui[6]: ""
  ui[7]: ""

  # Outputs
  uo[0]: "SPI_MISO"
  uo[1]: "RX_BYTE_VALID"
  uo[2]: "CRC_OK"
  uo[3]: "PACKET_DETECTED"
  uo[4]: "TX_ACTIVE"
  uo[5]: "SYMBOL_CLK"
  uo[6]: "MARK_GT_SPACE"
  uo[7]: "SAMPLE_CLK"

  # Bidirectional pins
  uio[0]: ""
  uio[1]: ""
  uio[2]: ""
  uio[3]: ""
  uio[4]: ""
  uio[5]: ""
  uio[6]: ""
  uio[7]: ""

# Do not change!
yaml_version: 6
